# Assumptions

- Raw datasets in `data/raw/` are treated as immutable inputs.
- Generated artifacts belong in `data/processed/` and `outputs/`.
- The notebook in `notebooks/` is the main entry point for phase 1 exploration.
